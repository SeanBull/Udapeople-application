---
- name: "deploy backend"
  become_user: true
  unarchive:
    src: /artifact.tar.gz
    dest: ~/

- name: "run backend"
  become_user: true
  shell: |
    cd ~
    npm i
    pm2 start npm -- run start
